#pragma once

#include "Header.h"

class MainDialog
{
public:
	void OnInitDialog(HWND hwnd);	

private:

	/*static DWORD WINAPI ThreadProc1(_In_ LPVOID lpParameter);
	static DWORD WINAPI ThreadProc2(_In_ LPVOID lpParameter);
	static DWORD WINAPI ThreadProc3(_In_ LPVOID lpParameter);*/

};
